# Reflection for Lab 1
What did you like/not like about Python
> I did not know Python while every people who learnt Python said it is super easy and is the best language to start coding with.
So I basically would not dislike it, and no surprise its syntax is very neat and easy, also with a lot of built-in functions that are ready to use, so that we can concentrate on coding on algorithms.

Was there anything that behaved differently than you expected in Python?
> Not at all. The only thing I did not expect is that it is pretty close to a "simplified" human language rather than coding language. I really love it now.

Based on what you wrote in your lab, write something about the similarities and differences between Python with C/C++ and how that affects how you write your program.
> There are a lot of differences based on the syntax and the writing style between Python and C/C++. C and C++ are very strongly typed language, and it is always challenging to build and compile them, while it has different types of runtime and compile time errors. By using Python, it is so much alike to Javascript which is user friendly.
The most identical difference is that, C/C++ must explicitly define the data types ahead and you need to spend time on memory allocation/ planning. Meanwhile, Python is completely different, it is just like Javascript, you don't even have to define the data type and it is smart enough to do that for you, not to mention memory leaks.
The similiarity between C++ and Python could be they are both OO programming languages which uses classes and objects.